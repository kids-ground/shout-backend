spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shout?characterEncoding=UTF-8mb4
    username: root
    password: rlafhrdnjs1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

server:
  port: 80